<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>expense-tracker</title>
  <!-- link css files here -->
  <link rel="stylesheet" href="/stylesheets/bootstrap.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid" style="text-align:center">
      <a class="navbar-brand" style="margin-left:47vw;font-size:25px" href="/">{{#if isAuthenticated}}{{ user.name}}'s {{/if}}家庭記帳本</a>
      </a>
      {{#if isAuthenticated}}
      <a href="/users/logout" class="btn border rounded" style="margin-right:5px;font-size:20px;">登出</a>
      {{else}}
      <a href="/users/login" class="btn border rounded" style="margin-right:5px;font-size:20px;">登入</a>
      {{/if}}
    </div>
  </nav>

 
<!--partial template -->
{{{body}}}

  <!-- include javascript files here -->
  <script src="/javascripts/popper.js"></script>
  <script src="/javascripts/bootstrap.js"></script>
  <script src="https://kit.fontawesome.com/3e50ed94d1.js" crossorigin="anonymous"></script>
</body>

</html>